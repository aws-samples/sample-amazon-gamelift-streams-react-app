// import * as cdk from 'aws-cdk-lib';
// import { Template } from 'aws-cdk-lib/assertions';
// import * as AmazonGameliftStreamsReactStarter from '../lib/amazon-gamelift-streams-react-starter-stack';

// example test. To run these tests, uncomment this file along with the
// example resource in lib/amazon-gamelift-streams-react-starter-api-stack.ts
test('Stream Created', () => {
//   const app = new cdk.App();
//     // WHEN
//   const stack = new AmazonGameliftStreamsReactStarter.AmazonGameliftStreamsReactStarterStack(app, 'MyTestStack');
//     // THEN
//   const template = Template.fromStack(stack);

//   template.hasResourceProperties('AWS::SQS::Queue', {
//     VisibilityTimeout: 300
//   });
});
